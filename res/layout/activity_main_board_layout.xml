<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#ffffff" >

    <ScrollView
        android:layout_width="fill_parent"
        android:layout_height="match_parent"
        android:layout_marginBottom="50dp" >

        <LinearLayout
            android:id="@+id/main_content_layout"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:background="#00000000"
            android:gravity="center"
            android:orientation="vertical" >
        </LinearLayout>
    </ScrollView>

    <RadioGroup
        android:id="@+id/main_radiogroup"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:layout_alignParentBottom="true"
        android:background="@color/main_gray"
        android:orientation="horizontal" >

        <RadioButton
            android:id="@+id/main_start"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:button="@null"
            android:checked="true"
            android:padding="0dp"
            android:drawableTop="@drawable/main_button_select"
            android:gravity="center"
            android:text="首页"
            android:textSize="11sp"
            android:textColor="@drawable/text_select" />

        <RadioButton
            android:id="@+id/main_product"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:padding="0dp"
            android:button="@null"
            android:drawableTop="@drawable/product_select"
            android:gravity="center"
            android:text="产品"
            android:textSize="11sp"
            android:textColor="@drawable/text_select" />

        <RadioButton
            android:id="@+id/main_utils"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:button="@null"
            android:drawableTop="@drawable/utils_select"
            android:gravity="center"
            android:padding="0dp"
            android:text="工具"
            android:textSize="11sp"
            android:textColor="@drawable/text_select" />

        <RadioButton
            android:id="@+id/main_user"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:button="@null"
            android:padding="0dp"
            android:drawableTop="@drawable/more_select"
            android:gravity="center"
            android:text="设置"
            android:textSize="11sp"
            android:textColor="@drawable/text_select" />
    </RadioGroup>

</RelativeLayout>